---
import SonicFieldPrototype from "../../../components/prototypes/SonicFieldPrototype.astro";

export const prerender = true;
---

<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width" />
    <title>Hearing Frequency Test · Sonic Field v3</title>
  </head>
  <body>
    <SonicFieldPrototype tool="hearing" />
  </body>
</html>

<style>
  @media (max-width: 760px) {
    :global(.sonic--hearing .frequency-path) {
      top: 178px;
    }

    :global(.sonic--hearing .observation-line) {
      bottom: 126px;
    }

    :global(.sonic--hearing .hearing-rail) {
      grid-template-columns: repeat(3, minmax(0, 1fr));
    }

    :global(.sonic--hearing .hearing-rail > .setup-group),
    :global(.sonic--hearing .hearing-rail > .action-group) {
      grid-column: 1 / -1;
    }

    :global(.sonic--hearing .hearing-rail > .rail-group:nth-child(2)) {
      border-right: 1px solid #c2c7c3;
    }

    :global(.sonic--hearing .hearing-rail > .rail-group:nth-child(4)) {
      border-right: 0;
    }
  }
</style>
